{
  "name": "andydonnell-com",
  "version": "1.0.0",
  "description": "Andy Donnell's portfolio website",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:css": "tailwindcss -i ./src/css/main.css -o ./static/styles.css --watch",
    "build:css:prod": "tailwindcss -i ./src/css/main.css -o ./static/styles.css --minify",
    "lint": "echo 'Linting not configured yet'",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "tailwindcss": "^3.4.0",
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.0",
    "@tailwindcss/typography": "^0.5.0"
  },
  "dependencies": {
    "@tailwindcss/cli": "^0.1.2"
  }
}
